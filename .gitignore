# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local runtime state (real credentials, usage history, logs — never commit)
*.log
.claude-unlimited/

# Tool-specific working directories not part of the shipped project
.impeccable/
graphify-out/
.claude/

# Internal-only documents — kept locally, never published.
# Everything below stays on this machine on purpose: it is either working
# material, internal decision history, or not relevant to someone using or
# contributing to the project.
docs/archive/
CONTEXT.md
PRODUCT.md
docs/adr/0003-consumer-oauth-pooling-and-export-crypto-on-hold.md
docs/adr/0004-user-accepted-risk-and-scope-decisions.md

# No changelog until the first tagged release.
CHANGELOG.md

# Planning material — tickets, specs, design notes, working plans. Local
# working documents, never published.
docs/tickets/
*.ticket.md
*.plan.md
codex_usage.md
docs/wiki/
tools/diagnose.py
# Scratch: reference PDFs, screenshots, design material. Was NOT ignored, and
# a design-system PDF had already been staged for commit from here.
temp/
tools/screenshot_fixtures.py
tools/nobanner.py

# The backend ships one runtime dependency (cryptography) and one dev extra
# (pytest), both pinned by range in pyproject.toml -- that file is the
# dependency source of truth, and nothing here installs from a lockfile.
# uv.lock shows up as an untracked byproduct of running uv locally;
# committing it would imply a locked build this project does not have.
uv.lock
